# 철거의정석 (cheolgeo) — 프로젝트 안내

인천 거점 철거 플랫폼 "철거의정석"의 저장소. 두 가지가 들어 있다.

1. **고객용 랜딩페이지**: `index.html`, `style.css`, `main.js` (정적, 라이브러리 없음, 라이트/다크 토글)
2. **마케팅팀 ERP**: 설계서 `docs/ERP-설계.md`, 프로토타입 `erp/index.html`, 인수인계 `docs/HANDOFF.md`

## 먼저 읽을 것
- `docs/HANDOFF.md` — 지금까지 결정된 것, 프로토타입 기능, 미결 질문, 다음 작업
- `docs/ERP-설계.md` — 역할·흐름·보고 템플릿·알림 라우팅·데이터 모델·스택·로드맵

## 실행
정적 파일이라 빌드 없음. `python3 -m http.server 8080` 후 `/erp/` 또는 `/` 접속. 또는 HTML을 브라우저로 직접 열기.

## 규칙
- 언어: UI 문구와 문서는 한국어. 코드 식별자는 영어.
- `erp/index.html`은 단일 파일 프로토타입. 외부 스크립트 로드 금지(인라인 유지). 상태 구조를 바꾸면 `seed()`의 `version`과 `load()`의 검사값을 같이 올린다.
- 색은 `:root` 토큰으로만. 라이트/다크(`prefers-color-scheme` + `[data-theme]`) 둘 다 확인.
- 휴대폰 폭(400px)에서 가로 스크롤이 생기면 안 됨.
- 알림 토큰(웹훅 URL, 봇 토큰)을 저장소에 커밋하지 않는다.
- 랜딩페이지(`index.html`)는 ERP 작업 중 건드리지 않는다.
